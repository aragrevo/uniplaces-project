---
const {src, alt, height, width, class: className, lazy = true} = Astro.props;
---

<img
  class={`rounded-lg object-cover object-top transition-all ${className}`}
  src={src}
  alt={alt}
  decoding='async'
  loading={lazy ? 'lazy' : 'eager'}
  fetchpriority='high'
/>
